# LightRAG Web 应用环境变量配置模板
# 复制此文件为 .env 并填入实际值

# ==================== 服务器配置 ====================
# HOST=0.0.0.0
# PORT=8000
# DEBUG=True
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ==================== LLM 配置 ====================
# LLM API 提供商（openai, anthropic, 等）
LLM_BINDING=openai
# LLM 模型名称
LLM_MODEL=deepseek-ai/DeepSeek-R1-0528-Qwen3-8B
# LLM API Key（必需，请从环境变量或 .env 文件配置）
LLM_BINDING_API_KEY=your_llm_api_key_here
# LLM API 主机地址
LLM_BINDING_HOST=https://api.siliconflow.cn/v1
# 最大并发请求数
MAX_ASYNC=2
# 请求超时时间（秒）
TIMEOUT=400

# ==================== Embedding 配置 ====================
# Embedding 提供商
EMBEDDING_BINDING=siliconcloud
# Embedding 模型名称
EMBEDDING_MODEL=Qwen/Qwen3-Embedding-0.6B
# Embedding 维度
EMBEDDING_DIM=1024
# Embedding API 主机地址
EMBEDDING_BINDING_HOST=http://localhost:11434

# ==================== 文件上传配置 ====================
# 最大文件大小（字节，默认 50MB）
MAX_FILE_SIZE=52428800
# 每个对话最多文件数
MAX_FILES_PER_CONVERSATION=20

# ==================== 图片渲染配置 ====================
# 图片分辨率（DPI）
IMAGE_RESOLUTION=150
# 图片缓存过期时间（小时）
# IMAGE_CACHE_EXPIRY_HOURS=24
# 是否启用图片缓存
# ENABLE_IMAGE_CACHE=True

# ==================== 样本试题配置 ====================
# 每个对话最多样本试题数
# MAX_SAMPLES_PER_CONVERSATION=50

# ==================== Gitee PaddleOCR 配置 ====================
# 是否启用 Gitee OCR（用于 PDF 解析）
# 设置为 true 以使用 Gitee PaddleOCR-VL 服务进行 PDF 解析
ENABLE_GITEE_OCR=true
# Gitee OCR Token（必需，如果启用 Gitee OCR）
# 获取方式：https://ai.gitee.com/serverless-api?model=PaddleOCR-VL（找到 PaddleOCR-VL → 在线体验 → API → 勾选"添加另外为内嵌代码"，每日100页免费额度）
GITEE_OCR_TOKEN=your_gitee_ocr_token_here
# OCR 请求超时时间（秒）
# GITEE_OCR_TIMEOUT=30
# OCR 最大重试次数
# GITEE_OCR_MAX_RETRY=2
# OCR 轮询间隔（秒）
# GITEE_OCR_POLL_INTERVAL=5
# OCR 最大等待时间（秒）
# GITEE_OCR_MAX_WAIT=60



